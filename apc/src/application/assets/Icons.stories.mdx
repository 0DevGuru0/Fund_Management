import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { IconsGallery } from './IconsGallery';

<Meta title="Atoms / Icons" component={IconsGallery} />

# SVG Icons

We use SVG icons in our application.
You can directly import svg files in any module.
Please put icons that can be used in more than 1 component in assets directory.

<Canvas>
  <IconsGallery />
</Canvas>
